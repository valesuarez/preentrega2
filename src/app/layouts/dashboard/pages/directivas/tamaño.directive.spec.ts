import { TamanoDirective } from './tamano.directive';

describe('TamanoDirective', () => {
  it('should create an instance', () => {
    const directive = new TamanoDirective();
    expect(directive).toBeTruthy();
  });
});
